# 大赛记账应用开发说明

## 开发环境

- OpenHarmony 5.0(API 19)
- DevEco Studio 5.1.1
- ArkTS + ArkUI
- Preferences 本地持久化

## 功能实现

### 任务 1：记账首页与数据持久化

- 首页实现文件：`entry/src/main/ets/views/HomeView.ets`
- 数据仓库：`entry/src/main/ets/repository/LedgerRepository.ets`
- 持久化工具：`entry/src/main/ets/Util/StorageUtil.ets`

支持收入/支出切换、金额、分类、备注、时间字段，支持新增、编辑、删除和列表实时刷新。所有账单数据以 JSON 字符串写入 Preferences，不使用数据库。

### 任务 2：报表可视化与预算预警

- 报表实现文件：`entry/src/main/ets/views/ReportView.ets`
- 统计计算：`entry/src/main/ets/store/AppStore.ets`

支持日、周、月收支统计，支出分类占比，近七日收入/支出趋势，自定义月预算与分类预算。总预算或分类预算超额时自动显示红色预警样式。

### 任务 3：个人中心与登录态管理

- 个人中心：`entry/src/main/ets/views/ProfileView.ets`
- 用户与会话仓库：`entry/src/main/ets/repository/UserRepository.ets`

支持用户信息展示、本地数据导出、账单清空、安全退出和账号切换。用户列表、登录态、账单、预算和分类预算均写入 Preferences，应用重启后自动恢复，超过有效期后自动退出。

### 任务 4：登录与注册

- 登录页面：`entry/src/main/ets/pageComp/loginView.ets`
- 注册页面：`entry/src/main/ets/pageComp/registerComp.ets`

支持账号密码校验、注册后自动登录、登录失败提示。密码使用简易哈希后保存，避免直接明文落盘。

## 主要目录

```text
entry/src/main/ets/
├── entryability/EntryAbility.ets
├── pages/MainEntry.ets
├── pageComp/
│   ├── Index.ets
│   ├── loginView.ets
│   └── registerComp.ets
├── views/
│   ├── HomeView.ets
│   ├── ReportView.ets
│   └── ProfileView.ets
├── model/RecordModel.ets
├── repository/
│   ├── LedgerRepository.ets
│   └── UserRepository.ets
├── store/
│   ├── AppStore.ets
│   └── GlobalStore.ets
├── common/Formatters.ets
├── utils/CryptoUtil.ets
└── Util/StorageUtil.ets
```

## 构建说明

使用 DevEco Studio 打开 `E:\DevEcoStudioProjects\universityCompetition`，同步工程后直接 Run 或通过 `Build > Build Hap(s)/APP(s) > Build Hap(s)` 生成安装包。
